<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secondhand.admin.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.secondhand.admin.pojo.domain.SysRole">
            <id property="id" column="id" />
            <result property="roleName" column="role_name" />
            <result property="roleKey" column="role_key" />
            <result property="sort" column="sort" />
            <result property="status" column="status" />
            <result property="delFlag" column="del_flag" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,role_name,role_key,sort,status,del_flag,
        create_time,update_time
    </sql>

    <select id="selectRoleKeysByUserId" resultType="java.lang.String">
        SELECT DISTINCT r.role_key
        FROM sys_role r
        LEFT JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND r.status = 1
        AND r.del_flag = 0
    </select>
</mapper>
